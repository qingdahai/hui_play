<!--pages/merchant/merchantDataModify/merchantLabel.wxml-->
<view class="page">
    <view class="list">
        <block wx:for="{{list}}" wx:for-item="item" wx:key="**this">
            <text bindtap="selected" data-id="{{item.id}}" class="{{item.active}}">{{item.name}}</text>
        </block>
        <!-- <view class="flex text" bindtap="userDefined">
            <image src="/images/add_icon.png"></image>
            <view>自定义</view>
        </view> -->
    </view>
    <button bindtap="clickon">确定</button>
</view>